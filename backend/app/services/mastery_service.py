def calculate_mastery(scores):
    if not scores:
        return 0

    return sum(scores)/len(scores)

def detect_weak_topic(user_id):
    if score < 70:
        return 
    
def readiness(topic):
    skill_score=*0.4 +
    interview_score=*0.4 +
    progress_score=*0.2


